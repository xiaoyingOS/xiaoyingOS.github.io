// Coroutine implementation

#include "best_server/coroutine/coroutine.hpp"
#include <thread>

namespace best_server {
namespace coroutine {

// Task implementations are header-only for templates

// FutureAwaiter implementations are header-only

// async function implementations are header-only

} // namespace coroutine
} // namespace best_server